First, go to main file C:/Motify/Go

After making changes, stage the changes:
$	git add .

Make a commit with summry msg:
$ 	git commit -m "Implemented new features in main.go"

Push back to Github (i.e. back to the remote repository you cloned from, on GitHub) 
and what exactly to push (your new branch).
$	git push origin master



To build the go file while in C/Motify/Go:
$	go build src/Resume/resume.go

To run the program:
$	./resume